interface Color {
  red: number;
  green: number;
  blue: number;
}

const color: Color = {
  red: 20,
  green: 10,
  blue:10,
};

console.log(`Color - R:${color.red} G:${color.green} B:${color.blue}`);
