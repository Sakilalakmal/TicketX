import request from "supertest";
import { app } from "../../app.js";

it("returns a 201 on successful ticket creation", async () => {

});

it("it can be only be access user will be signed in", async () => {

});

it("it return an error if invalid title is provided", async () => {

});

it("it return an error if invalid price is provided", async () => {

});

it("create a ticket with valid parameters", async () => {
    
});
